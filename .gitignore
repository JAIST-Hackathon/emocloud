# ------------------------------------------------------------------------------
# Next.js プロジェクト向け .gitignore
# バージョン管理から除外すべきファイルとディレクトリを定義します。
# これらは通常、自動生成されるファイル、一時ファイル、
# 環境固有の設定、または機密情報です。
# ------------------------------------------------------------------------------

# --- 1. ビルド成果物とキャッシュ ---
# Next.js がビルドプロセス中に生成するファイルやキャッシュディレクトリ。
# これらはソースコードから再生成可能であり、リポジトリに含めるべきではありません。
.next/             # Next.js のビルド出力ディレクトリ
.next/cache/       # Next.js のキャッシュディレクトリ
.next/types/       # Next.js が生成する型定義（自動生成されるため無視）
out/               # Next.js の静的エクスポート出力ディレクトリ (使用する場合)
dist/              # 一般的なビルド出力ディレクトリ

# --- 2. 依存関係の管理 ---
# パッケージマネージャーがインストールする依存関係。
# `npm install` や `yarn install` でいつでも再生成可能です。
node_modules/      # Node.js モジュールのインストールディレクトリ

# --- 3. 環境変数ファイル ---
# APIキー、データベース接続情報など、機密情報を含む可能性のある環境変数ファイル。
# これらは各開発環境で個別に管理されるべきです。
.env               # 一般的な環境変数ファイル
.env.local         # ローカル開発環境専用の環境変数ファイル
.env.*.local       # .env.development.local, .env.production.local など
.env.development.local
.env.test.local
.env.production.local

# --- 4. ログと診断レポート ---
# アプリケーションの実行中に生成されるログファイルやデバッグ情報。
logs/              # ログディレクトリ
*.log              # すべてのログファイル
npm-debug.log* # npm のデバッグログ
yarn-debug.log* # Yarn のデバッグログ
yarn-error.log* # Yarn のエラーログ
pnpm-debug.log* # pnpm のデバッグログ
.report/           # Node.js 診断レポートディレクトリ

# --- 5. OS および エディタ/IDE 固有のファイル ---
# オペレーティングシステムや開発ツールが自動生成するファイル。
# これらはプロジェクトのソースとは無関係であり、開発者間で共有する必要はありません。
.DS_Store          # macOS の Finder メタデータ
Thumbs.db          # Windows のサムネイルキャッシュ
ehthumbs.db        # Windows の拡張サムネイルキャッシュ
[Dd]esktop.ini     # Windows のフォルダ表示設定
.vscode/           # VS Code のワークスペース設定ディレクトリ
# !.vscode/settings.json # VS Code の共有設定を含める場合（チームで合意した場合のみ）
# !.vscode/tasks.json    # VS Code の共有タスクを含める場合
# !.vscode/launch.json   # VS Code の共有デバッグ設定を含める場合
# !.vscode/extensions.json # VS Code の推奨拡張機能を含める場合
# !.vscode/*.code-snippets # VS Code の共有スニペットを含める場合
# !*.code-workspace      # VS Code のワークスペースファイルを含める場合
.idea/             # JetBrains IDEs (WebStorm, IntelliJ IDEA など) の設定ディレクトリ
*.iml              # JetBrains IDEs のモジュールファイル
*.swp              # Vim/Neovim のスワップファイル
*~                 # Emacs やその他のエディタのバックアップファイル
.cache/            # 各種ツールのキャッシュディレクトリ
.history/          # 一部のエディタのファイル変更履歴

# --- 6. その他の一時ファイル ---
# アプリケーション実行時に一時的に生成されるファイル。
*.tmp              # 一時ファイル全般
temp/              # 一時ファイルを格納する共通ディレクトリ
tmp/               # 一時ファイルを格納する共通ディレクトリ

# --- 7. テレメトリー関連 (Next.js 固有) ---
# Next.js の利用状況に関するテレメトリー設定ファイル。
.next/telemetry/   # Next.js テレメトリー設定

# --- 8. 機密情報、バイナリファイルなど（必要に応じて追加） ---
# アプリケーションで使用される可能性のある機密情報や、バージョン管理不要なバイナリファイル。
*.key              # 秘密鍵ファイル
*.pem              # PEM形式の証明書/鍵ファイル
*.p12              # P12形式の証明書/鍵ファイル
# my_project_specific_data.db # プロジェクト固有のローカルDBファイルなど
# /user_uploads/             # ユーザーアップロードディレクトリなど
node_modules
.next